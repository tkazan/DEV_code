1. W Pythonie każdy element ma wartośc True lub False.
Żeby to sprawdzić możemy użyć funkcji bool().
Wszystkie puste elementy dają wartośc False.

2. set(zbiór) - to lista unikalnych elementów. Właściwie, to taki słownik z samymi kluczami, bez wartości --> {set}
set() możemy użyć na słowniku, aby dostać klucze.
set() różni się od list() tym, że ma tylko unikalne elementy i nie zachowuje kolejności.
Set jest szybszy od listy

3. Wbudowana w Pythona jest funkcja sum() - dodaj do siebie wszystkie zadane liczby

4. # rozpakowywanie słownika -->
def message(d):
    validation = {"name", "role", "movie"}
    if not validation.difference(input_dict):
        return "In {movie}, {name} is a {role}.".format(**input_dict) # <--tutaj
    else:
        return None

input_dict = {
    "name": "Han Solo",
    "role": "smuggler",
    "movie": "Star Wars"
}

5. IMPORTOWANIE
np. import random
random.randint(0, 10)

6. Sprawdzanie jakie są funkcje w module i co robi dana funkcja
import random
dir(random) # pokazuje dostępne funkcje w module random
help(random.randint) # pokazuje prosty opis wybranej funkcji

7. Jeżeli mamy pliki w tym samym folderze, to możemy zaimportować funkcje, zmienne itp.
z tego pliku do innego. Poprzez import nazwa_pliku
Aby w nowym pliku nie wykonywały się od razu funkcje z oryginalnego, na końcu oryginalnego pliku wpisujemy:
if __name__ == "__main__": 
