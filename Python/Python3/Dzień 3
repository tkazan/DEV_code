1. Żeby uzyskać osobno każde słowo ze stringa możemy użyć funkcji split()
text = "To jest jakiś string"
splitted = text.split()
print(splitted) --> ["To", "jest", "jakiś", "text"]

2. dodawanie do słowników
d = {}
d['names'] = 1 # dodaje wartość: 1 do klucza: 'names'

3. dwie funkcje w słowniku: get i setdefault

d = {}
r = d.setdefault(40, [])
--> r = []
print(d) --> {40:[]}

d[10] = 'nowe logo'
d.setdefault(20, []) --> {10:'nowe logo', 20:'[]}
d.setdefault(10, []) --> 

4. OBSŁUGA WYJĄTKÓW: TRY, EXCEPT

try:
    # tutaj komendy, które mogą spowodować wyjątek
    pass
    
except Exception as e:
    print("Houston mamy problem, WYJĄTEK!", e)
    
5. RZUCANIE WYJĄTKIEM - raise

np. raise ValueError("This is not integer")

6. DEKORATORY

Dekorator to funkcja, która jako argument pobiera inną funkcję.
Działa jak wrapper (opakowanie), który opakowuje funkcję dodając jej
jakieś właściwości.



